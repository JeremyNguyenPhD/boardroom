{
  "name": "boardroom",
  "version": "1.1.0",
  "description": "Split-screen interface for ChatGPT, Claude, Gemini, Grok, Kimi, and DeepSeek",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test test/static.test.js test/link-actions.node.test.js test/popup-controls.node.test.js test/popup-page.node.test.js",
    "build": "electron-builder",
    "build:mac": "electron-builder --mac",
    "build:win": "electron-builder --win"
  },
  "build": {
    "appId": "com.boardroom.app",
    "productName": "Boardroom",
    "files": [
      "src/**/*",
      "package.json"
    ],
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": ["universal"]
        }
      ],
      "category": "public.app-category.productivity"
    },
    "win": {
      "target": ["nsis"]
    },
  "nsis": {
      "artifactName": "${productName}-Setup-${version}.${ext}",
      "oneClick": true,
      "deleteAppDataOnUninstall": false
    }
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^38.2.2",
    "electron-builder": "^26.0.0"
  }
}
